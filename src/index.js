import getCustomerName from './updateCustomerProfile'
// Import other function files as needed

const bcHelper = {
  getCustomerName,
  // Add other functions as properties of the bcHelper object
};

export default bcHelper;